- Install node modules for server and frontend

  ```
  cd server && yarn
  ```

  ```
  cd frontend && yarn
  ```

- Start backend server
  ```
  cd server && yarn start
  ```
- Start backend server
  ```
  cd frontend && yarn dev
  ```
